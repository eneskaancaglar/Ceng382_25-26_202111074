@model TasteAtDoor.Models.ViewModels.ReviewListPageViewModel

@{
    ViewData["Title"] = "Ratings";
}

<div class="dashboard-shell">
    <div class="page-header-row">
        <div>
            <h1 class="page-title">Ratings and Comments</h1>
            <p class="page-subtitle">
                Inspect package ratings, caterer ratings, comments, and completed-request feedback.
            </p>
        </div>

        <a asp-controller="Admin" asp-action="Dashboard" class="btn-secondary-main">
            Back to Dashboard
        </a>
    </div>

    <div class="table-card">
        <form method="get" asp-controller="Admin" asp-action="Ratings" class="row g-3">
            <div class="col-md-3">
                <label class="form-label">Search</label>
                <input name="search"
                       value="@Model.Search"
                       class="form-control"
                       placeholder="Request, package, customer, caterer, comment..." />
            </div>

            <div class="col-md-2">
                <label class="form-label">Package Rating</label>
                <input name="menuRating"
                       value="@Model.MenuRating"
                       type="number"
                       min="1"
                       max="5"
                       class="form-control" />
            </div>

            <div class="col-md-2">
                <label class="form-label">Caterer Rating</label>
                <input name="catererRating"
                       value="@Model.CatererRating"
                       type="number"
                       min="1"
                       max="5"
                       class="form-control" />
            </div>

            <div class="col-md-2">
                <label class="form-label">Start Date</label>
                <input name="startDate"
                       type="date"
                       value="@(Model.StartDate.HasValue ? Model.StartDate.Value.ToString("yyyy-MM-dd") : "")"
                       class="form-control" />
            </div>

            <div class="col-md-2">
                <label class="form-label">End Date</label>
                <input name="endDate"
                       type="date"
                       value="@(Model.EndDate.HasValue ? Model.EndDate.Value.ToString("yyyy-MM-dd") : "")"
                       class="form-control" />
            </div>

            <div class="col-md-1 d-flex align-items-end">
                <button type="submit" class="btn-main w-100">Filter</button>
            </div>
        </form>

        <div class="mt-3">
            <a asp-controller="Admin"
               asp-action="Ratings"
               class="btn-secondary-main">
                Clear Filters
            </a>
        </div>
    </div>

    <div class="table-card">
        @if (!Model.Reviews.Any())
        {
            <p>No ratings found.</p>
        }
        else
        {
            <table class="custom-table">
                <thead>
                    <tr>
                        <th>Request</th>
                        <th>Customer</th>
                        <th>Caterer</th>
                        <th>Catering Package</th>
                        <th>Package Rating</th>
                        <th>Caterer Rating</th>
                        <th>Comment</th>
                        <th>Date</th>
                    </tr>
                </thead>

                <tbody>
                    @foreach (var review in Model.Reviews)
                    {
                        <tr>
                            <td>#@review.OrderId</td>

                            <td>
                                @review.CustomerName
                                <br />
                                <small>@(review.CustomerEmail ?? "-")</small>
                            </td>

                            <td>
                                @review.CatererName
                                <br />
                                <small>@(review.CatererEmail ?? "-")</small>
                            </td>

                            <td>@review.MenuItemName</td>
                            <td>@review.MenuRating / 5</td>
                            <td>@review.CatererRating / 5</td>
                            <td>@(string.IsNullOrWhiteSpace(review.Comment) ? "-" : review.Comment)</td>
                            <td>@review.CreatedAt.ToString("dd MMM yyyy HH:mm")</td>
                        </tr>
                    }
                </tbody>
            </table>
        }

        <div class="d-flex justify-content-between align-items-center mt-3">
            <div>Page @Model.Page / @Model.TotalPages</div>

            <div class="d-flex gap-2">
                @if (Model.Page > 1)
                {
                    <a class="btn-secondary-main"
                       asp-controller="Admin"
                       asp-action="Ratings"
                       asp-route-search="@Model.Search"
                       asp-route-menuRating="@Model.MenuRating"
                       asp-route-catererRating="@Model.CatererRating"
                       asp-route-startDate="@(Model.StartDate.HasValue ? Model.StartDate.Value.ToString("yyyy-MM-dd") : null)"
                       asp-route-endDate="@(Model.EndDate.HasValue ? Model.EndDate.Value.ToString("yyyy-MM-dd") : null)"
                       asp-route-page="@(Model.Page - 1)">
                        Previous
                    </a>
                }

                @if (Model.Page < Model.TotalPages)
                {
                    <a class="btn-secondary-main"
                       asp-controller="Admin"
                       asp-action="Ratings"
                       asp-route-search="@Model.Search"
                       asp-route-menuRating="@Model.MenuRating"
                       asp-route-catererRating="@Model.CatererRating"
                       asp-route-startDate="@(Model.StartDate.HasValue ? Model.StartDate.Value.ToString("yyyy-MM-dd") : null)"
                       asp-route-endDate="@(Model.EndDate.HasValue ? Model.EndDate.Value.ToString("yyyy-MM-dd") : null)"
                       asp-route-page="@(Model.Page + 1)">
                        Next
                    </a>
                }
            </div>
        </div>
    </div>
</div>